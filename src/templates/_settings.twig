{% import '_includes/forms.twig' as forms %}

{{ forms.selectizeField({
    first: true,
    label: "Volumes"|t('_hosted-assets'),
    instructions: "The volumes where the hosted video assets are stored."|t('_hosted-assets'),
    id: 'volumeHandles',
    name: 'volumeHandles',
    options: volumeOptions,
    multi: true,
    values: settings.volumeHandles,
}) }}

{{ forms.lightSwitchField({
    label: "Hide Upload Button"|t('_hosted-assets'),
    instructions: "Hide the \"Upload files\" button on asset index screens."|t('_hosted-assets'),
    id: 'hideUploadButton',
    name: 'hideUploadButton',
    on: settings.hideUploadButton,
}) }}
